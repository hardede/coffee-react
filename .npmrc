# Public npm registry
registry=https://registry.npmjs.org/

# Auth token from env (optional; do not hardcode secrets)
//registry.npmjs.org/:_authToken=${NPM_TOKEN}

# Keep lockfile package URLs normalized
replace-registry-host=never

# NOTE:
# If you need a proxy in CI/Vercel, set HTTP(S)_PROXY in platform settings,
# do not put unresolved placeholders in .npmrc (it breaks npm install).
